# Copie ce fichier en .env.local et renseigne tes valeurs Supabase.
# Ces valeurs sont aussi à configurer dans Vercel/Netlify (variables d'environnement).
# La clé anon est publique côté client — n'utilise PAS la service_role key ici.

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
